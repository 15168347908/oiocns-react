import { model } from '@/ts/base';
import { ITransfer } from '@/ts/core';
import React from 'react';

interface IProps {
  transfer: ITransfer;
  current: model.Store;
}

const DataModal: React.FC<IProps> = () => {
  return <></>;
};

export default DataModal;
